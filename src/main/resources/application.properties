spring.application.name=proyectoSpringBoot

# Configuracion a la base de Datos a
spring.datasource.url=jdbc:mysql://localhost:3306/organizacionbd
spring.datasource.username=campus2023
spring.datasource.password=campus2023
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configuracion inicializacion base de datos

# spring.sql.init.mode=always
# spring.sql.init.schema-locations=import.sql
# Configuracion JPA
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update

# Configuración del registro de seguridad
logging.level.org.springframework.security=DEBUG
